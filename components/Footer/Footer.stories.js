import Footer from '.'

export default {
  title: 'Screens/Footer',
  component: Footer
}

const Template = (args) => <Footer {...args} />

export const Default = Template.bind({});

Default.args = {}